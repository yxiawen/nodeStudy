const PhotoModels = require('../model/PhotoModel.js')

class PhotoModels {
  // static imgUpload () {
  //   let auth = ctx.request.body
  //   if (auth) {
  //     let photo = {}
  //     photo.UserId = ctx.user.id
  //     photo.url = ctx.photo.url
  //     photo.albumId = ctx.photo.albumId
  //     photo.isDelete = ctx.photo.isDelete
  //     let save = await PhotoModels.save(photo)
  //     if (save) {
  //       ctx.body = {
  //         code: '200',
  //         status: '保存成功！'
  //       }
  //     }
  //   }
  // }
}